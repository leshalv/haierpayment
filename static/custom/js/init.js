require(['jquery', 'bootstrap', 'util', 'Const'], function ($, bootstrap, util, Const) {
    util.init();
    util.initEnums();
    // 初始化实体类定义
    // util.initEntities();
    // 字典
    // util.initDicts();+
    $('body').show();
});